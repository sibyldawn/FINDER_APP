import React from 'react';
import './Settings.css';

const Settings = () => {
    return (
        <div>
            <div className="settingsTitle">
                Settings
            </div>
        </div>
    );
};

export default Settings;